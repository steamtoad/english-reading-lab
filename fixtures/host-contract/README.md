# ERL host contract test double

This directory is an isolated, minimal host contract used only by ERL tests.
It is not a canonical Zettelkasten implementation and production ERL runtime
must never use it as an implicit fallback.
